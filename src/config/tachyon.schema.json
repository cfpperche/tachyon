{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/cfpperche/tachyon/src/config/tachyon.schema.json",
  "title": "Tachyon configuration (tachyon.yml)",
  "description": "Declares the agents and settings Tachyon orchestrates in this workspace.",
  "type": "object",
  "additionalProperties": false,
  "anyOf": [
    { "required": ["agents"], "properties": { "agents": { "minProperties": 1 } } },
    { "required": ["terminals"], "properties": { "terminals": { "minProperties": 1 } } }
  ],
  "properties": {
    "agents": {
      "description": "Agents (AI CLIs, dev servers, watchers \u2014 any command) keyed by name.",
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-zA-Z][a-zA-Z0-9_-]*$"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "cmd"
        ],
        "properties": {
          "cmd": {
            "description": "Shell command that runs the agent.",
            "type": "string",
            "minLength": 1
          },
          "cwd": {
            "description": "Working directory, relative to the workspace root or absolute.",
            "type": "string"
          },
          "env": {
            "description": "Extra environment variables for the agent's session.",
            "type": "object",
            "additionalProperties": {
              "type": "string"
            }
          },
          "autostart": {
            "description": "Start this agent when Tachyon starts.",
            "type": "boolean",
            "default": false
          },
          "watch": {
            "description": "Glob(s) \u2014 restart the agent when a matching file changes.",
            "oneOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                },
                "minItems": 1
              }
            ]
          },
          "restart": {
            "description": "Crash policy: on-crash auto-restarts when the process dies with a non-zero exit (backoff 2s/4s/8s, max 3 per minute). Clean exits (0) never auto-restart.",
            "enum": [
              "never",
              "on-crash"
            ],
            "default": "never"
          },
          "attention": {
            "description": "Needs-you detection. Defaults: on for kind=agent, off for kind=terminal. Object form tunes the idle threshold and adds prompt patterns.",
            "oneOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "enabled": {
                    "type": "boolean"
                  },
                  "silenceSec": {
                    "description": "Seconds of unchanged pane + idle CPU before the agent is marked idle.",
                    "type": "integer",
                    "minimum": 1
                  },
                  "patterns": {
                    "description": "Extra prompt regexes (case-insensitive) that mark the agent as needing input.",
                    "type": "array",
                    "items": {
                      "type": "string",
                      "minLength": 1
                    }
                  }
                }
              }
            ]
          },
          "kind": {
            "description": "agent = AI CLI (claude, codex, ...); terminal = server/shell/build. Default: inferred from cmd. Drives sidebar grouping and attention defaults.",
            "enum": [
              "agent",
              "terminal"
            ]
          },
          "instructions": {
            "description": "Role prompt for the agent (e.g. 'you are a code reviewer...'). Delivered as a positional argument on spawn for CLIs that accept one (claude, codex, gemini); stored but not auto-delivered otherwise.",
            "type": "string"
          },
          "role": {
            "description": "Built-in role template (spec 216): a reusable task contract composed with 'instructions' at delivery (template first). agents-only; not valid for a terminal.",
            "type": "string",
            "enum": ["coder", "reviewer", "tester", "orchestrator", "custom"]
          },
          "worktree": {
            "description": "Run this agent in its own git worktree + branch (opt-in, off by default). Isolates parallel agents so they don't clobber each other's files.",
            "type": "boolean",
            "default": false
          },
          "branch": {
            "description": "Literal branch name for this agent's worktree (overrides the global settings.worktree.branch template). Validated via git check-ref-format at creation.",
            "type": "string"
          },
          "worktreeSetup": {
            "description": "Command(s) run ONCE in the fresh worktree before the agent starts (sequential, stop-on-failure). $TACHYON_WORKSPACE_ROOT and $TACHYON_WORKTREE_ROOT are set.",
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "minItems": 1
              }
            ]
          },
          "verify": {
            "description": "Verify-gate (spec 214): a command/runbook name (or inline shell) run IN the worktree to prove it shippable. Resolves like a runbook step (command name > runbook name > inline). Advisory — surfaces a pass/fail/stale badge, never blocks.",
            "type": "string",
            "minLength": 1
          },
          "harness": {
            "description": "Isolated harness (spec 226/228/298): agent-scoped MCP servers + skills + rules + hooks, materialized into a private config home so they never leak to sibling agents. Claude/Codex only (a harness on another runtime/terminal entry is a config error). Declare at least one of mcp/skills/rules/hooks. Tachyon owns the config home + MCP plumbing, so Claude commands must not carry --mcp-config/--strict-mcp-config/--settings and env must not set CLAUDE_CONFIG_DIR or CODEX_HOME. NOTE: cross-field rules (cmd runtime, kind!=terminal, reserved flags/env, inherit!=global, ${VAR}-only mcp env, at-least-one-capability) are enforced by the loadConfig validator (not expressible in pure JSON Schema) — the validator is authoritative; this schema covers structure for editor hints.",
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "inherit": {
                "description": "Whether to seed the workspace base before overlaying. 'none' = clean slate (sees ONLY the declared config); 'workspace' = the workspace .mcp.json snapshot plus the declared servers. 'global' is a follow pass (not yet supported). Default: workspace.",
                "enum": ["none", "workspace"]
              },
              "mcp": {
                "description": "MCP servers scoped to THIS agent only (stdio shape). Each env value must be an exact ${VAR} reference — never a literal — so no secret is written to disk; claude expands it from the agent's process env at spawn.",
                "type": "object",
                "minProperties": 1,
                "propertyNames": { "pattern": "^[a-zA-Z][a-zA-Z0-9_-]*$" },
                "additionalProperties": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": ["command"],
                  "properties": {
                    "command": { "type": "string", "minLength": 1 },
                    "args": { "type": "array", "items": { "type": "string" } },
                    "env": {
                      "type": "object",
                      "additionalProperties": { "type": "string", "pattern": "^\\$\\{[A-Za-z_][A-Za-z0-9_]*\\}$" }
                    }
                  }
                }
              },
              "hooks": {
                "description": "spec 228 — a claude settings.json 'hooks' object, merged into the agent's private settings.json (scoped to this agent). Shape is the claude hooks shape; claude validates the contents.",
                "type": "object",
                "minProperties": 1
              },
              "rules": {
                "description": "spec 228 — rule file path(s), concatenated into the agent's private CLAUDE.md (its own behavioral context). Workspace-relative; a single string or a non-empty list.",
                "oneOf": [
                  { "type": "string", "minLength": 1 },
                  { "type": "array", "items": { "type": "string", "minLength": 1 }, "minItems": 1 }
                ]
              },
              "skills": {
                "description": "spec 228 — skill dir path(s) (each holding a SKILL.md), copied into the agent's private skills/ (resolve via /<name>). Workspace-relative; a single string or a non-empty list.",
                "oneOf": [
                  { "type": "string", "minLength": 1 },
                  { "type": "array", "items": { "type": "string", "minLength": 1 }, "minItems": 1 }
                ]
              }
            }
          }
        }
      }
    },
    "terminals": {
      "description": "Terminals (servers, shells, builds) keyed by name (spec 215). Same entry shape as an agent, but kind is always 'terminal' (so 'kind' and 'instructions' aren't meaningful here — loadConfig rejects them) and attention defaults off. The engine treats these identically to an agents: entry with kind: terminal — this block just reads more naturally.",
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-zA-Z][a-zA-Z0-9_-]*$"
      },
      "additionalProperties": { "$ref": "#/properties/agents/additionalProperties" }
    },
    "layouts": {
      "type": "object",
      "deprecated": true,
      "description": "Deprecated \u2014 the named-layouts feature was retired (use VS Code's native editor groups). This block is accepted for backward compatibility but ignored."
    },
    "settings": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "maxAgents": {
          "description": "Max concurrently running agents in this workspace (fork-bomb guardrail). Overrides the tachyon.maxAgents editor setting.",
          "type": "integer",
          "minimum": 1
        },
        "bridgePort": {
          "description": "Fixed port for the Bridge MCP server. Default: a stable per-workspace port derived from the workspace path (41000-42999) \u2014 registrations survive editor restarts either way.",
          "type": "integer",
          "minimum": 1024,
          "maximum": 65535
        },
        "auth": {
          "description": "Require a Bearer token on every Bridge call (default true). The stable per-workspace token lives in extension storage and is injected as TACHYON_BRIDGE_TOKEN into every agent Tachyon spawns; registered configs reference the env var, so no secret lands in committable files.",
          "type": "boolean",
          "default": true
        },
        "layout": {
          "type": "string",
          "deprecated": true,
          "description": "Deprecated — the named-layouts feature was retired. Accepted for backward compatibility but ignored."
        },
        "tmux": {
          "type": "object",
          "description": "tmux server options applied to Tachyon's dedicated socket as `set -g <key> <value>`. Tachyon's defaults (mouse on, focus-events on, history-limit 10000) apply first and this overlays them; 'remain-on-exit' is reserved (crash detection depends on it). Your ~/.tmux.conf is never loaded. Values may be booleans (on/off), numbers, or strings.",
          "additionalProperties": {
            "type": [
              "string",
              "number",
              "boolean"
            ]
          }
        },
        "worktree": {
          "type": "object",
          "description": "Global worktree settings (spec 210).",
          "additionalProperties": false,
          "properties": {
            "base": {
              "type": "string",
              "description": "Location root for worktrees; path = <base>/<wsHash>/<agent>. Default ~/.cache/tachyon/worktrees (XDG-aware)."
            },
            "branch": {
              "type": "string",
              "description": "Global branch-name template; MUST contain {agent}. Default tachyon/{agent}.",
              "pattern": ".*\\{agent\\}.*"
            },
            "verify": {
              "type": "string",
              "minLength": 1,
              "description": "Global default verify-gate (spec 214) for worktree agents that don't declare their own. A command/runbook name or inline shell; a per-agent verify wins."
            }
          }
        },
        "anchor": {
          "type": "object",
          "description": "Role re-anchoring (spec 216).",
          "additionalProperties": false,
          "properties": {
            "auto": {
              "type": "boolean",
              "default": false,
              "description": "Auto re-inject an agent's role reminder after a detected compaction (claude/codex only), on the next idle window. OFF by default — it types into the live terminal. The manual 'Re-anchor agent' command and .tachyon/ROLE.md work regardless."
            }
          }
        },
        "bridgeGuidance": {
          "type": "boolean",
          "default": true,
          "description": "Append Bridge-coordination guidance to agents spawned via the Bridge so delegated work stays visible in Tachyon (spec 216). Default true."
        },
        "clipboard": {
          "type": "string",
          "enum": ["auto", "off"],
          "default": "auto",
          "description": "Clean clipboard copy (spec 219). 'auto' wires a UTF-8 copy-mode helper so a plain mouse drag-select (incl. scrollback) copies clean — no Shift, mouse stays on. 'off' leaves the OSC 52 default (use if copy already works or on a headless box)."
        }
      }
    },
    "commands": {
      "description": "One-shot commands (test, lint, migrate...): run -> exit -> show result. Unlike agents/terminals, exiting is expected (exit 0 = pass). Agents can run them via the curated run_command tool.",
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-zA-Z][a-zA-Z0-9_-]*$"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "cmd"
        ],
        "properties": {
          "cmd": {
            "type": "string",
            "minLength": 1,
            "description": "Shell command; its exit code is the result."
          },
          "cwd": {
            "type": "string",
            "description": "Working directory (relative to workspace root or absolute)."
          },
          "env": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            }
          }
        }
      }
    },
    "runbooks": {
      "description": "Step-by-step procedures composed of commands. Steps run sequentially in their own sessions; a non-zero exit stops the runbook (the failed pane is kept). Steps are exact command-name references or inline shell strings.",
      "type": "object",
      "propertyNames": {
        "pattern": "^[a-zA-Z][a-zA-Z0-9_-]*$"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "steps"
        ],
        "properties": {
          "steps": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      }
    }
  }
}
