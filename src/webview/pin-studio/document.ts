/** spec 339 — moved to `../rich-doc/document.ts` (entity-neutral); re-exported here so existing pin-studio
 * imports (and its tests) keep resolving unchanged. */
export * from "../rich-doc/document.js";
