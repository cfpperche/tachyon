/** spec 339 — moved to `../rich-doc/data-url.ts` (entity-neutral); re-exported here so existing pin-studio
 * imports (and its tests) keep resolving unchanged. */
export * from "@tachyon/webview-ui/webview/rich-doc/data-url";
