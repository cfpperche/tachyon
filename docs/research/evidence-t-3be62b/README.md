# t-3be62b — Review binary viewers

Visual anchor: binary evidence is visible in Review, contained inside the diff pane, and legible in both themes. Raster and SVG retain their proportions; PDF presents a readable page; 3D presents an interactive viewport.

The eight captures are generated by the real built Review bundle through `test/browser/reviewBinaryVisual.test.ts`:

- `raster-{light,dark}.png`
- `svg-{light,dark}.png`
- `pdf-{light,dark}.png`
- `model-{light,dark}.png`

Fixtures are `sample.svg` (including script/onload negative controls), `sample.pdf`, `sample.gltf`, and the existing raster evidence from `evidence-t-91884b`.

Regenerate:

```sh
node esbuild.mjs
npx vitest run --config vitest.browser.config.ts test/browser/reviewBinaryVisual.test.ts
```
