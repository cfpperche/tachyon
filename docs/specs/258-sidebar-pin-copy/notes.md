# Spec 258 — Notes

- User correction during implementation: copy **ID + Title**, not ID + description.
- Clipboard payload is deliberately stable ASCII text:
  ```text
  ID: <pin-id>
  Title: <pin-title>
  ```
