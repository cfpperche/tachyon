# Spec 260 — Pin tags — tasks

- [x] Add `Pin.tags` storage contract and tag normalization tests.
- [x] Extend `PinStore` create/update/rich-detail flows to read/write normalized tags.
- [x] Extend Bridge pin schemas and tests without changing tool count.
- [x] Extend Pin Studio VM/save message and host routing with tags.
- [x] Build the Pin Studio tag editor UI.
- [x] Extend sidebar VM projection with tags.
- [x] Render pin tag chips and add tag filtering in the Pins tab.
- [x] Make Cmd/Ctrl+K pin search match tags and `#tag` queries.
- [x] Run unit, typecheck, build, and EDH dogfood.
- [x] Close the spec with validation evidence.
