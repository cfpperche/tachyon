# 283 — agent-screen-primitive · tasks

**Status:** deferred — no tasks until promoted (see spec.md § Deferral). The first "task" is the demand gate itself:
a real consumer needing non-web Visual QA.
