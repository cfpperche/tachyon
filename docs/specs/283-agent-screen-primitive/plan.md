# 283 — agent-screen-primitive · plan

**Status:** deferred — no plan until promoted by a real use case (see spec.md § Deferral).

When demand lands, `/sdd plan` from the spec. The build shape is sketched in spec.md (single-platform v1, fail-closed,
mirror `agent-browser`); the per-OS capture/a11y matrix is the main design work to do then, not now.
