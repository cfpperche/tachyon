# Spec 254 — tasks (build order)

Decomposed like specs 250/251: each step is implemented, then adversarially codex-reviewed before the next. MCP reuses the engine surface (manifest loader, `previewInstall`/`applyInstall`/`applyRemove`, lockfile `mcp-server` target — already reserved, consent VM, Plugins View) and **generalizes the existing Bridge MCP writers** in `src/registration/adapters.ts`. The codex required-changes checklist (spec.md § Codex debate adjudication) is the gate; each item is annotated `[codex #N]` below.

**Verify:** `env -u TMUX npx vitest run test/unit/pluginMcp.test.ts test/unit/pluginEngine.test.ts test/unit/pluginManifest.test.ts test/unit/pluginConsentViewModel.test.ts test/unit/adapters.test.ts && npx tsc --noEmit && npx tsc -p tsconfig.webview.json --noEmit && bash scripts/check-engine-boundary.sh`

## Steps

- [x] **Step 1 — neutral `mcp.json` payload + strict schema loader.** [codex #2, #3, #4] DONE: `src/plugins/mcp.ts` (pure `loadMcpPayload` + `mcpRequiredEnv`) + `engine.ts` (`MCP_FILE`, `LoadedPlugin.mcp`, `discoverMcp`, ≥1-capability count). Strict stdio|http union, fail-closed, caps, env exact-`${VAR}` (H7), http header = `[Bearer|Basic|Token] ${VAR}` (scheme allowlist — no hard-coded secret), `${PLUGIN_ROOT}` leading-token-contained, `requiresEnv` derived. Codex dueto: NEEDS-REVISION (2 HIGH: header secret-bypass + `__proto__` proto-pollution/derivation) → folded → confirming **SHIP**. ~28 mcp + engine MCP tests; full suite 1259 green; tsc ×2 + engine-boundary clean. New `src/plugins/mcp.ts` (pure): load a top-level `mcp.json` payload at the plugin root (auto-discovered like `skills/` — OQ1), validate a **strict neutral union schema** (OQ2): `{ name, transport: "stdio"|"http", … }` where stdio = `{command, args[], env}` and http = `{url, headers}`. Apply the same fail-closed treatment as `manifest.ts`: known-key closure (no unknown keys), resource caps (max servers / arg count / string length), no controls/nulls, server `name` against the `NAME_RE` kebab regex. **Secrets (D4):** reject any literal secret-bearing value — `env` values, `headers.Authorization`, bearer fields must be structural `${VAR}` references only; **derive the required-env list from those refs** (no separate `requiresEnv` — OQ3). **`${PLUGIN_ROOT}` (OQ4):** allowed ONLY as a leading token in path-like fields (`command`, a path-shaped arg), suffix validated by `paths.ts`/`validBlockPath` containment (no absolute, `..`, backslash, controls); NO general string substitution inside arbitrary args. `loadPlugin` already enforces "≥1 capability"; extend it to count an `mcp.json` payload. Tests: schema accept/reject matrix (both transports, secret-literal rejection, `${PLUGIN_ROOT}` escape rejection, caps). Codex dueto before Step 2.
- [x] **Step 2 — per-runtime MCP planner + pure format renderers.** DONE: `engine.ts` (`AdapterSpec.mcpRel`, `runtimeSupportsMcp`, `McpTarget`, `planMcpTargets` — mirrors planSkillTargets); `adapters/claude.ts renderClaudeMcpEntry` (verbatim `mcpServers.<name>`); `adapters/codex.ts renderCodexMcpBlock` (`[mcp_servers.<name>]` TOML, `tomlStr`-escaped, auth → `bearer_token_env_var`/`env_http_headers`, env → `env_vars`). Refined the Step-1 header model (Bearer only on Authorization; env key==var) for lossless dual-runtime mapping. Codex dueto: NEEDS-REVISION (HIGH: codex `env` doesn't expand `${VAR}` → use `env_vars`) → folded → confirming **SHIP**. Suite 1270 green; tsc ×2 + boundary clean. `runtimeSupportsMcp(rt)` + `AdapterSpec` MCP destinations (claude `.mcp.json`, codex `.codex/config.toml`; null = no MCP loader → skipped, declare-and-skip). `planMcpTargets(plugin, present)` — PURE, mirroring `planSkillTargets`: each declared server × each present MCP-capable runtime → `{runtime, server, destRel}`, in `SUPPORTED_RUNTIMES` order. Pure per-runtime **render** functions: neutral server → claude `mcpServers.<name>` JSON entry (stdio/http) and codex `[mcp_servers.<name>]` TOML block (stdio `command/args/env`; http `url/bearer_token_env_var/http_headers`), mapping the env-ref idiom per runtime. Collision/merge/I-O stays in Step 4. Tests: planner skip/order + render golden for both transports × both runtimes. Codex dueto.
- [ ] **Step 3 — generalize the shared writer + PIN the Bridge first.** [codex #5, #9 — D6] **First**, add Bridge regression tests in `test/unit/adapters.test.ts` pinning current behavior (stale-port overwrite, other-server preservation, exact `mcpServers.tachyon` / `[mcp_servers.tachyon]` names, idempotent no-op, `TACHYON_BRIDGE_TOKEN` indirection, TOML targeted-block-only edit) — the regression net BEFORE refactor. **Then** lift the merge / targeted-TOML-range / already-registered mechanics out of `registration/adapters.ts` into ONE server-name-generic, **escaping-safe** writer: JSON via `JSON.stringify` (safe); TOML via a **kebab name regex + value-escape** (the current `url = "${url}"` interpolation is safe only because `tachyon` is hard-coded — untrusted names/values must be encoded). Re-point the Bridge registration at the shared writer (its tests must stay green, unchanged behavior). Codex dueto.
- [ ] **Step 4 — engine `mcp-server` install/remove path (I/O).** [codex #6, #7 — D5/D7] `previewInstall` plans `mcpTargets` (each with a `collision` flag = a server name exists at the dest that isn't this plugin's prior `mcp-server` target) and binds them into the consent fingerprint; `applyInstall` takes `mcpDecisions` (per-dest `keep|replace`), **fails closed on an undecided collision**, renders + merges each consented server via the Step-3 writer, records `mcp-server` lockfile targets (`ref` = server name, `removal` = the expected rendered entry/hash for content-based un-merge), and on update **cleans up stale servers** the new version dropped. `applyRemove` un-merges exactly the recorded servers by name — preserving every other server/line/comment — never a user's own. Reuse the TOCTOU fingerprint (install + remove) + per-file lost-update check. **`validMcpDest`/target validation** gates every lockfile trust/delete path (mirror `validSkillDest` — a corrupted lockfile can never delete/overwrite a non-MCP path or a user's server). Tests: install/skip per runtime, stdio+http, collision Keep, collision Replace, undecided fail-closed, content-preserving removal, corrupted-target safe degrade. Codex dueto (expect a security round).
- [ ] **Step 5 — consent VM + drawer MCP section + double-confirm EVERY install.** [codex #8 — OQ5] `consentViewModel.ts` gains `ConsentVM.mcp` (per-server: name, transport, exact command+args or url, **referenced env-var names**, destinations) + `mcpCollisions`, populated in `buildInstallConsent`/`buildUpdateConsent`. The drawer (`App.tsx`) renders an **MCP servers** section (showing argv/url + env refs + dest) + a per-collision **Keep mine / Replace** control (default Keep) + a **mandatory second-confirmation ack for ANY MCP install** (OQ5 = stronger than skills' Replace-only; Install stays disabled until acked; Replace adds its own ack). Decisions thread `confirm → {mcpDecisions} → confirmOp → applyInstall/applyUpdate`; the engine fails closed on undecided collisions (the real net). `key={consent.token}` remount so a new consent resets ack state (the spec-251 Step-4 leak). UI proven by driving the real built bundle (screenshot install + replace states). Codex dueto.
- [ ] **Step 6 — live dogfood.** Extend the example repo (`github.com/cfpperche/tachyon-plugin-example`) with an `mcp.json` payload (a stdio server + an http server) at a new tag. Dogfood the full lifecycle **headless against the real git repo**: install writes the server into BOTH claude `.mcp.json` (`mcpServers.<name>`) and codex `.codex/config.toml` (`[mcp_servers.<name>]`); remove un-merges exactly those, leaving other servers intact; a deliberate collision with a pre-existing user server → Keep leaves it, Replace overwrites, undecided is refused; the double-confirm gate is exercised. Never auto-enable — each runtime's own approval is the final gate (OQ6).

## Closure
**Closure:** _(filled at ship — mirror spec 251's closure: capability summary, per-step codex rounds, test count + suite-green at close, and what was deferred.)_
