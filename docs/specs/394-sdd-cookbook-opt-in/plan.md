# 394 — plan

Implement in **tachyon-plugins** repo (`sdd/`), not by hand-editing materialized skills.

1. Template `cookbook.md.tmpl` + helper `sdd-cookbook.sh`
2. `sdd-close.sh` warning-only cookbook hygiene (mirror visual QA)
3. `SKILL.md` + `README.md` + tasks template opt-in section
4. Regression `test-cookbook-close.sh`; keep `test-visual-close.sh` green
5. Bump plugin to 1.5.0; human installs; dogfood with 392 cookbook
