# Disposable Dev Host fixture — t-4a2a6f: Authorize / Reauthorize / Authorized.
#
# t-f67185 — empty agents alone is loadable. The shell terminal remains for a REAL reason: run
# ./drift.sh without leaving the EDH (not a roster non-empty workaround).
#
# Every agent in this scenario is created through Agent Studio — starting from zero agents is the point.
settings:
  maxAgents: 4

agents: {}
terminals:
  shell:
    cmd: sh
    autostart: false
