agents:
  echoer:
    cmd: sh
    autostart: true
    watch: "trigger.txt"
  prompter:
    cmd: sh
    autostart: true
  flaky:
    cmd: sh
    autostart: true
    restart: on-crash
    attention: false
layouts:
  solo:
    grid: 2up
    agents: [echoer]
