# SDD 478 M7 — `cmd: sh` is a process, so this row was never an agent; multi-root resolution is
# what the fixture proves, and a terminal proves it identically.
terminals:
  alpha-agent:
    cmd: sh
    autostart: true
commands:
  alpha-check:
    cmd: "echo alpha-ok"
