---
name: house-style
description: Dev Host fixture skill — exists so the Agent Studio's Authorize control has something real to pin. Not a plugin skill; it is hand-written in this workspace, which is the origin the authorization door classifies as `workspace`.
---

# house-style

A workspace skill with no upstream. Authorizing it pins the digest of this tree, because there is no
plugin version to name — the content IS the provenance.
