---
name: demo-stable
description: Authorized by `bystander`. Never changes, so bystander's profile must keep loading.
---

# demo-stable

`bystander` authorizes this one. Its bytes never move, so `bystander` holds a pinned reference that
stays valid through the update — which is what makes it a real control rather than an agent that
merely has nothing to lose.
