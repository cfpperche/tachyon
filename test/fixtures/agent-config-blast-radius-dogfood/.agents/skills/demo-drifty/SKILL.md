---
name: demo-drifty
description: Authorized by `pinned`. ./drift.sh moves it, so pinned's profile is refused at load.
---

# demo-drifty v1

`pinned` authorizes this one. After `./drift.sh` the reference names bytes that no longer exist and
that profile is refused — correctly. The fixture exists to check what the refusal costs everyone
else.
