import { describe, expect, it } from "vitest";

describe("container-generated delegation behavior", () => {
  it("a queued notice carries its source child's incarnation and a poke from a dead incarnation is dropped even after a same-name respawn", () => {
    expect.fail("delegation not implemented yet");
  });
});
