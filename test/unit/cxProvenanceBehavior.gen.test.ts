import { describe, expect, it } from "vitest";

describe("container-generated delegation behavior", () => {
  it("a build from an uncommitted tree self-reports dirty and a tampered dist fails its provenance record, while an unrecorded clean build stays silent", () => {
    expect.fail("delegation not implemented yet");
  });
});
